[project]
name = "flowns-research"
version = "0.1.0"
description = "Flow Matching Negative Sampling research code."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26.0",
    "pandas>=2.2.0",
    "rich>=13.7.0",
    "torch>=2.2.0",
    "wandb>=0.17.0",
]

[tool.uv]
package = false
