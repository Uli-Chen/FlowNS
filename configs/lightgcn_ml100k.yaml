# LightGCN + MovieLens-100K sanity checks
model: LightGCN
dataset: ml-100k
data_path: /home/chen/workspace/RecBole/dataset
load_col:
  inter: [user_id, item_id, rating, timestamp]
n_layers: 3
reg_weight: 1.e-4
require_pow: false

# Small dense dataset; keep batches moderate for autoencoder sanity runs.
train_batch_size: 2048
eval_batch_size: 409600
